# apigw-cnl-cross — API Gateway
# Generated by Jarvis Platform
# Copiar a .env y completar los valores marcados con ***

# ── Servidor ──────────────────────────────────────────────
PORT=10601
NODE_ENV=development

# ── CORS ──────────────────────────────────────────────────
# Orígenes permitidos separados por coma
# En development se acepta cualquier origen automáticamente
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:10100

# ── Rate Limiting ─────────────────────────────────────────
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
REQUEST_TIMEOUT=120000

# ── JWT (validación local — S6) ───────────────────────────
# Debe coincidir EXACTAMENTE con JWT_SECRET del auth service (ms-cnl-cross-auth-profile).
# El gateway valida el token localmente sin llamar al auth service.
# En producción usar un valor aleatorio largo (mínimo 32 chars).
JWT_SECRET=***

# ── Servicios proxiados ───────────────────────────────────
# URL del Auth Service — usada para PROXY de rutas /auth/*
AUTH_URL=http://localhost:10701
# MS Canal cross-cutting — PROXY de rutas /audit/*, /catalogs/* y /media/*
CNL_AUDIT_URL=http://localhost:10703
CNL_CATALOGS_URL=http://localhost:10704
CNL_MEDIA_URL=http://localhost:10702

# ── SSL ───────────────────────────────────────────────────
# USE_SSL=true activa el servidor HTTPS adicional en SSL_PORT
USE_SSL=false
SSL_PORT=20601
# Ruta donde están los certificados (server.crt / server.key o server.pfx)
CERT_PATH=/app/certs
SERVER_NAME=

# ── Kafka Audit Logger ────────────────────────────────────
# AUDIT_LOGGER_ENABLED=false desactiva el audit logger por completo:
# no se intenta conectar a Kafka y el logging se vuelve un no-op silencioso.
# Default: true (si no se define, el audit logger queda habilitado).
AUDIT_LOGGER_ENABLED=true
# Broker(s) Kafka separados por coma
KAFKA_BROKER=localhost:9092
# Topic al que este servicio publica eventos de gateway
KAFKA_TOPIC=platform.logs
